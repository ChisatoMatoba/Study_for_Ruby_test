<div class="container">
  <div class="content">
    <h1>カテゴリ作成</h1>

    <%= form_with(model: @category, local: true) do |form| %>
      <div class="form-group mb-3">
        <%= form.label :name, 'カテゴリ名', class: "form-label" %>
        <%= form.text_field :name, class: 'form-control', placeholder: '例：Ruby Silver' %>
      </div>
      <div class="form-group">
        <%= form.submit "作成", class: "btn btn-primary mb-3" %>
      </div>
    <% end %>

    </div>
  </div>
</div>
